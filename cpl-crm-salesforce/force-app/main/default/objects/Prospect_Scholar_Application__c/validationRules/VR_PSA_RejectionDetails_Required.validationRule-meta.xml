<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>VR_PSA_RejectionDetails_Required</fullName>
    <active>true</active>
    <description>Ensures the the rejection details are filled out when a status is rejected or withdrawn.</description>
    <errorConditionFormula>AND(
  OR(
    ISPICKVAL(Application_Status__c, &quot;Rejected&quot;),
    ISPICKVAL(Application_Status__c, &quot;Withdrawn&quot;)
  ),
  OR(
    ISBLANK(TEXT(Rejection_Filter__c)),
    ISBLANK(Rejection_Reason__c)
  )
)</errorConditionFormula>
    <errorDisplayField>Rejection_Filter__c</errorDisplayField>
    <errorMessage>Rejection Filter and Rejection Reason are required when an application is Rejected or Withdrawn.</errorMessage>
</ValidationRule>
